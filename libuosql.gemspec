Gem::Specification.new do |s|
    s.name          = 'libuosql'
    s.version       = '0.0.0'
    s.summary       = "library to connect to uosql-server."
    s.description   = "library to connect to uosql-server."
    s.files         = ["lib/libuosql.rb"]
end